import { PageHeader } from "@/components/page-header";
import { ProgressBar } from "@/components/progress-bar";
import { vocabulary } from "@/data/mock-data";

export default function ReviewPage() { return <main className="page safe-top"><PageHeader eyebrow="Spaced review" title="Weak vocabulary"/><p className="-mt-4 mb-7 leading-relaxed text-slate-600">A focused set of words that need another look. Small repetitions make them stick.</p><div className="space-y-4">{vocabulary.map(word => <article className="card p-5" key={word.id}><div className="flex items-start justify-between"><div><h2 className="text-2xl font-extrabold">{word.dutch}</h2><p className="mt-1 text-sm text-slate-500">/{word.pronunciation}/</p></div><span className="rounded-full bg-orange-50 px-3 py-1 text-sm font-extrabold text-[#e8672e]">{word.mastery}%</span></div><p className="mt-4 font-bold text-slate-700">{word.english}</p><div className="my-4"><ProgressBar value={word.mastery}/></div><div className="flex gap-6 text-xs font-bold text-slate-500"><span><b className="text-emerald-700">{word.correctCount}</b> correct</span><span><b className="text-red-600">{word.incorrectCount}</b> incorrect</span></div></article>)}</div></main>; }
