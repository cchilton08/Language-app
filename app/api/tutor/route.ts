import { NextResponse } from "next/server";
export const runtime="nodejs";
type H={role:"assistant"|"user";text:string};
function out(d:any){if(typeof d?.output_text==="string")return d.output_text;const p:string[]=[];for(const i of d?.output??[])for(const c of i?.content??[])if(c?.type==="output_text")p.push(c.text);return p.join("\n")}
const schema={type:"object",properties:{reply:{type:"string"},translation:{type:"string"},hint:{type:"string"},correction:{anyOf:[{type:"null"},{type:"object",properties:{wrong:{type:"string"},better:{type:"string"},why:{type:"string"},clue:{type:"string"}},required:["wrong","better","why","clue"],additionalProperties:false}]},retryRequired:{type:"boolean"},learnedChunks:{type:"array",items:{type:"object",properties:{dutch:{type:"string"},english:{type:"string"},pronunciation:{type:"string"},memoryHook:{type:"string"}},required:["dutch","english","pronunciation","memoryHook"],additionalProperties:false}}},required:["reply","translation","hint","correction","retryRequired","learnedChunks"],additionalProperties:false};
export async function POST(req:Request){const key=process.env.OPENAI_API_KEY;if(!key)return NextResponse.json({error:"OPENAI_API_KEY is not configured."},{status:503});try{const b=await req.json();const history:H[]=Array.isArray(b.history)?b.history.slice(-16):[];const known=Array.isArray(b.knownWords)?b.knownWords.slice(0,180):[];const weak=Array.isArray(b.weakWords)?b.weakWords.slice(0,30):[];const focus=Array.isArray(b.focusWords)?b.focusWords.slice(0,16):[];const mistakes=Array.isArray(b.commonMistakes)?b.commonMistakes.slice(-20):[];const transcript=history.map(x=>`${x.role==="user"?"LEARNER":"TUTOR"}: ${x.text}`).join("\n");const instructions=`You are Calvin's Dutch speaking coach for STANDARD NETHERLANDS DUTCH. His structured curriculum is Learn Dutch with Kim / Dutchies to Be. THE APP IS NOT A COMPETING COURSE.\n\nKnown course material:\n${known.join("\n")||"No recorded material yet."}\n\nRecent lesson material: ${focus.join(", ")||"none"}\nWeak material: ${weak.join(", ")||"none"}\nRecurring mistakes: ${JSON.stringify(mistakes)}\n\nRULES:\n- Primarily use vocabulary, phrases, grammar and structures in the known material. Recycle older material deliberately.\n- Make him PRODUCE Dutch himself. Ask one concise question at a time; do not turn the conversation into a lesson or answer for him.\n- Rough target: 90% known/comprehensible language. Occasionally introduce ONE transparent, useful new word naturally, but do not flood him with unlearned Dutch.\n- Increase Dutch and reduce English gradually as demonstrated ability improves.\n- Correct only meaningful errors. Minor punctuation/capitalization is irrelevant. If meaning is clear and Dutch is natural, accept it.\n- When correcting, briefly explain the issue and invite a retry. Never leak a hidden answer in a hint.\n- Prefer recent and weak course material, but mix older items so they cannot disappear.\n- Keep replies short and conversational, suitable for speaking aloud.\n- learnedChunks should normally be EMPTY because new curriculum comes from Kim. Only add one if a genuinely necessary, naturally encountered word was introduced. Never save incomplete phrases or ellipses.\n- translation is a concise English translation of reply; hint is only a partial cue.\n- Natural Netherlands usage, not textbook oddities.`;const r=await fetch("https://api.openai.com/v1/responses",{method:"POST",headers:{Authorization:`Bearer ${key}`,"Content-Type":"application/json"},body:JSON.stringify({model:process.env.OPENAI_MODEL||"gpt-5.6-luna",instructions,input:`${transcript}\nLEARNER: ${String(b.message||"").slice(0,1400)}`,max_output_tokens:900,text:{verbosity:"low",format:{type:"json_schema",name:"course_conversation",strict:true,schema}}})});const d=await r.json();if(!r.ok)return NextResponse.json({error:d?.error?.message||"OpenAI request failed"},{status:r.status});const text=out(d).trim();const p=JSON.parse(text.slice(text.indexOf("{"),text.lastIndexOf("}")+1));return NextResponse.json({...p,learnedChunks:Array.isArray(p.learnedChunks)?p.learnedChunks.filter((x:any)=>x?.dutch&&!String(x.dutch).includes("...")&&!String(x.dutch).includes("…")).slice(0,1):[]})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Unexpected tutor error"},{status:500})}}
